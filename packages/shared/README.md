# packages/shared

Shared modules used by multiple runtimes.

- `backend/`: backend helpers consumed by `apps/exam-api`, `apps/panel-api`, `apps/ops-api`

This package is intentionally runtime-agnostic and has no route ownership.
